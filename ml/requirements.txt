# ─────────────────────────────────────────────────────────────────────────────
# ml/requirements.txt — dépendances du service ML
# ─────────────────────────────────────────────────────────────────────────────
# Le package shared/ est installé séparément via `pip install -e ../shared/`
# (voir Dockerfile et README).
#
# Versions auditées et alignées (nov 2024 - mars 2025) pour Python 3.11/3.12 :
#   - mlflow 2.20.x : dernière 2.x stable, supporte numpy 2 (depuis 2.18)
#   - numpy 2.2.x   : compatible mlflow 2.18+, pandas 2.3, sklearn 1.5
#   - huggingface_hub ~=0.36 : dernière 0.x, API stable (avant breaking changes v1)
#   - tout le reste : versions cohérentes entre elles, pas de conflit
# ─────────────────────────────────────────────────────────────────────────────

# ── I/O données ───────────────────────────────────────────────────────────────
pandas==2.3.3
pyarrow>=16.0,<19            # parquet engine (resserré pour reproductibilité)
huggingface_hub~=0.36.0      # dernière 0.x stable (API compatible avec notre code)
tenacity>=8.2,<9             # retry exponentiel

# ── ML — versions cohérentes avec la formation MLOps ──────────────────────────
numpy==2.2.6                 # bumpé : 2.1.3 → 2.2.6 pour compatibilité mlflow 2.20
scikit-learn==1.5.2          # supporte numpy 2 depuis 1.5.0
xgboost==3.0.2
optuna==3.6.1
lightgbm

# ── Tracking ──────────────────────────────────────────────────────────────────
mlflow==2.20.3               # bumpé : 2.15.1 → 2.20.3 (résout conflit numpy 2)

# ── Versioning données (DVC + DagsHub remote S3-compatible) ──────────────────
dvc[s3]>=3.50,<4

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib==3.9.2            # conservé pour rétro-compat / debug
seaborn==0.13.2              # idem
plotly>=5.20,<7              # graphes interactifs (HTML autonome + Streamlit)
kaleido>=0.2.1,<2            # export PNG/SVG des figures Plotly (bonus)

# ── Tests ────────────────────────────────────────────────────────────────────
pytest>=8.0,<9
pytest-cov>=5.0,<7
